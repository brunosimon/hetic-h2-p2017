<div class="container">
<section>
    {% for page in pages %}
        {% if page.current %}
            Page {{page.number}}
        {% else %}
            <a href="{{url('categorie',{category:page.category,page:page.number})}}">Page {{page.number}}</a>
        {% endif %}
    {% endfor %}
</section>
</div>