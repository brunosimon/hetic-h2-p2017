<section class="pagination">
    {% for page in pages %}
        {% if page.current %}
            <span class="pageNumber">{{page.number}}</span>
        {% else %}
            <a class="pageNumber" href="{{url('page',{page:page.number})}}">{{page.number}}</a>
        {% endif %}
    {% endfor %}
</section>
