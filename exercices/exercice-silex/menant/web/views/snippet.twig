{{include('inc/header.twig')}}

<section class="base wrap add-snippet snippet-page">
	<h3>{{snippet.title}}</h3>
	<a class="trs btn btn-cat" href="{{url('category',{category:snippet.category_slug})}}">{{snippet.category_title}}</a>
	{% if app.session.get('admin') %}<a class="trs btn btn-cat-mod" href="{{url('admin/snippet/update', {snippet:snippet.id})}}">Modify</a><a class="trs btn btn-cat-del" href="{{url('admin/snippet/delete', {snippet:snippet.id})}}">Delete</a>{% endif %}
	<textarea name="content" id="snippet_add">{{snippet.content}}</textarea>
	</form>
</section>

{{include('inc/footer.twig')}}