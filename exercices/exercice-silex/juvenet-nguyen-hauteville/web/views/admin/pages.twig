{{include('inc/header.twig')}}
{{include('inc/menu.twig')}}
{{include('inc/message.twig')}}

<div class="admin row">
  {{include('admin/menu.twig')}}
  <div class="col-md-9">
    <div class="panel panel-default">
      <div class="panel-body">
        <form class="form-horizontal" method="POST">
          <fieldset>
            <div class="col-lg-12">
              {% for page in pages %}
              <div class="form-group">
                <div class="well well-sm pages">
                  {{page.title}}
                </div>
                <textarea class="form-control pages" rows="6" id="textArea" name="page[{{page.id}}]">{{page.content}}</textarea>
              </div>
              {% endfor %}
              <div class="form-group">
                <button type="submit" class="btn btn-primary">Submit</button>
              </div>
            </div>
          </fieldset>
        </form>
      </div>
    </div>
  </div>
</div>

{{include('inc/footer.twig')}}