<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <!-- Bootstrap -->
  <link href="{{app.request.basePath}}/src/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="{{app.request.basePath}}/src/css/default.css">

  <title>Snippet</title>
</head>

<body>
  <div class="navbar navbar-inverse">
  	<div class="navbar-header">
    	<a class="navbar-brand" href="{{url('home')}}">Hetic Snippets</a>
  	</div>

  	<div class="navbar-collapse collapse navbar-responsive-collapse">
    	<ul class="nav navbar-nav">
    		<li class=""><a href="{{url('home')}}">Home</a></li>
	      <li class=""><a href="{{url('about')}}">About</a></li>
	      <li class="dropdown">
	       	<a href="#" class="dropdown-toggle" data-toggle="dropdown">Catégories <b class="caret"></b></a>
          <ul class="dropdown-menu">

            {# dynamic recovery of categories which already exists and number of snippets in each of them#}
            {% for categories in categories_count %}
              <li><a href="{{url('category',{category:categories.slug})}}">{{categories.title}} ({{categories.total_snippets_by_category}})</a></li>
            {% endfor %}

            <li><a href="{{url('add_category')}}">Ajouter une catégorie</a></li>
          </ul>
	      </li>
      	<li><a href="{{url('add_snippets_post')}}">Ajouter snippets</a></li>
    	</ul>


    <ul class="nav navbar-nav navbar-right">
      <li><a href="{{url('contact_post')}}">Contact</a></li>
    </ul>
  </div>
</div>
